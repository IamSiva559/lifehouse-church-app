import { StyleSheet, Text, View } from "react-native";
import { StatusBar } from "expo-status-bar";

const ConnectGroupScreen = () => {
  return (
    <View>
      <Text>ConnectGroupScreen</Text>
    </View>
  );
};

export default ConnectGroupScreen;

const styles = StyleSheet.create({});
